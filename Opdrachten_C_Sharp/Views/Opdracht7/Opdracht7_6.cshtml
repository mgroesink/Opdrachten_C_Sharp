@model int[]
De gegenereerde array: <br/>
@for(int i = 0; i < Model.Length; i++)
{
    <span>@Model[i] ; </span>
}
<br/>
In omgekeerde volgorde:<br/>
@for(int i = Model.Length - 1; i >= 0 ; i--)
{
    <span>@Model[i] ; </span>
}