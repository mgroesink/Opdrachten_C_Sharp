@model int[]

<h2>Alle even getallen</h2>
<ul>
@for(int i = 0 ; i < Model.Length ; i++)
{
    if(Model[i] % 2 == 0)
    {
        <li>@Model[i]</li>
    }
}
</ul>

<h2>Alle oneven getallen</h2>
<ul>
@for(int i = 0 ; i < Model.Length ; i++)
{
    if(Model[i] % 2 == 1)
    {
        <li>@Model[i]</li>
    }
}
</ul>
<br/>
<h2>In totaal zijn er @Model.Length getallen, waarvan @Model.Count(m=>m % 2 == 0) even
    en @Model.Count(m=>m % 2 == 1) oneven.
</h2>